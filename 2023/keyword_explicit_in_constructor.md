```
explicit vector( size_type count, const T& value = T(), const Allocator& alloc = Allocator());  (until C++11)
         vector( size_type count, const T& value,  const Allocator& alloc = Allocator()); (since C++11) (until C++20)
         
constexpr vector( size_type count, const T& value,  const Allocator& alloc = Allocator()); (since C++20)
explicit vector( size_type count );
```
